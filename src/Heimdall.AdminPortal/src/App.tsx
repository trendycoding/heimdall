/**
 * App component is no longer used directly.
 * The Layout component serves as the app shell.
 * See main.tsx for the application entry point and routes.tsx for routing.
 */
export default function App() {
  return null;
}
